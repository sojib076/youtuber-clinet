import React from 'react';
import DynamicTitle from '../../Hook/DynamicTitle';

const Blog = () => {
    DynamicTitle('Blog')
    return (
        <div>
         


</div>
           
    );
};

export default Blog;